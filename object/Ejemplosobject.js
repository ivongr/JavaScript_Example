//se crea un objeto y lo asigna a la variable x
let x;
let cond = true;
if (cond){ //si es verdadero
    x = { greeting: "hi there"}; // se le asigna este valor a x
}
console.log(x);

// objeto con tres propiedades
const myHonda = {
    color: "red",
    wheeles: 4,
    engine: {cylinders:4, size:2.2},
};
console.log(myHonda);

